tcs_core_homepage:
    path:     /accueil
    defaults:
        _controller: TCSCoreBundle:Core:index

tcs_core_stream:
    path: /stream/{streamer}
    defaults:
        _controller: TCSCoreBundle:Core:stream

